apiVersion: v1
kind: Service
metadata:
  name: book-store-inventory-ms
  labels:
    app: book-store-inventory-ms
spec:
  type: LoadBalancer
  ports:
  - name: http
    protocol: TCP
    port: 8100
    targetPort: 8100
  - name: debug
    protocol: TCP
    port: 5005
    targetPort: 5005
  selector:
    app: book-store-inventory-ms